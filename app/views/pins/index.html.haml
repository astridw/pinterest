#pins.transitions-enabled
  - @pins.each do |pin|
    .box.panel.panel-default
      = link_to (image_tag pin.photo.url()), pin
      .panel-body
        %ul= link_to pin.title, pin
        %p.user
          Submitted by
          = pin.user.email
